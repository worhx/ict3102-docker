version: '3'
services:
    react:
        build:
            context: react
            dockerfile: Dockerfile
        ports:
            - "80:3000"
        volumes: ['./react/:/app']
        entrypoint:
            - npm
            - start
        networks: 
            - temp_network
networks:
  temp_network:
